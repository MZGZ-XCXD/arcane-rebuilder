# 统一行尾为 LF：避免 Windows 编辑器 / 网页上传把行尾改成 CRLF，
# 导致每次改动都出现「整文件都变了」的 diff。
* text=auto eol=lf

# 二进制文件不做行尾转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
